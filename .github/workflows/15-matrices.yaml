name: 15-matrices
on: 
  workflow_dispatch

jobs:
  backwards-compatibility:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        node-version:
          - 18.x
          - 20.x
          - 21.x
        os:
          - ubuntu-latest
          - windows-latest
        
        include:
          - os: ubuntu-latest
            tag: linux
    steps:
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      
      - name: Fail if experimental
        if: 

      - name: Perform some tests
        run: echo "Running tests on OS ${{matrix.os}} and ${{matrix.node-version}}"
