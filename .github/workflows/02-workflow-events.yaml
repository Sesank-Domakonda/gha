name: 02-workflow-events
on: 
  push:
  workflow_dispatch: 
  schedule:
    - cron: '0 0 1 1 *'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: ${{github.event_name}}
